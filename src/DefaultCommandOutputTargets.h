#ifndef __AUDACITY_DEFAULT_COMMAND_OUTPUT_TARGETS__
#define __AUDACITY_DEFAULT_COMMAND_OUTPUT_TARGETS__

#include <memory>
class CommandOutputTargets;

std::unique_ptr<CommandOutputTargets> DefaultCommandOutputTargets();

#endif
